server {
    listen 80;

    # Serve the Vue.js SPA — all unknown paths fall back to index.html for client-side routing.
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html;
    }
}
